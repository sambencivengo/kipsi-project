import { Inter } from '@next/font/google';
import { Box } from '@chakra-ui/react';

const inter = Inter({ subsets: ['latin'] });

export default function Home() {
	return <Box>This is the base app page! Please use the navbar for </Box>;
}
